const fullName: string = 'NVAKhoa'

// TS sẽ gợi ý hàm
console.log(fullName.length)

// Như JS, TS cũng có các kiểu dữ liệu của JS 
// Nguyên thủy: String, number, boolean, null, symbol, bigint
// Tham chiếu: object, array, functions