enum API_STATUS {
    PENDDING = "PENDDING", //0
    FULFILLED = "FULFILLED", //1
    REJECTED = "REJECTED" //2
}

let a1 = API_STATUS.PENDDING;
let a2 = API_STATUS.FULFILLED;

console.log(a1, a2);
